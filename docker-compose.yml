services:
  mongodb:
    image: mongo:latest
    container_name: documind_mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=documind_db

volumes:
  mongo_data:
